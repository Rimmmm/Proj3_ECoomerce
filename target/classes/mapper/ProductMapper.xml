<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProductMapper">
	<!-- 상품 리스트 보기 -->
	<select id="listProduct" resultType="proj.ecom.product.domain.ProductDTO">
		select * from ecommerce_product  where product_itemType=#{product_itemType} and product_sortType=#{product_sortType} order by product_id desc
	</select>
	
	<!-- 상품 자세히 보기 -->
	<select id="productDetail" resultType="proj.ecom.product.domain.ProductDTO">

		SELECT product_itemType, product_sortType, product_name, product_image, product_price 
		FROM ecommerce_product where product_id=#{product_id}
	</select>
	
	<!-- 상품 사이즈 및 수량 확인하기 -->
	<select id="productDetail2" resultType="proj.ecom.product.domain.ProductDTO">
		select product_size, product_quantity from ecommerce_productSize where product_id=#{product_id}
	</select>
	
	
	<!-- <select id="productDetail" resultType="proj.ecom.product.domain.ProductDTO">

		SELECT ep.product_itemType, ep.product_sortType, ep.product_name, ep.product_image, ep.product_price, eps.product_size, eps.product_quantity
		
		FROM ecommerce_product ep INNER JOIN ecommerce_productSize eps 
		ON ep.product_id = eps.product_id 
		where eps.product_id=#{eps.product_id}
	</select> -->
	
	<!-- productSize테이블에서 사이즈와 수량을 가져와야함. 조건은 product_id   -->
	<!-- select product_size, product_quantity from ecommerce_productSize where product_id=#{product_id}-->


<!-- 	<select id="listDao" resultType="spring.mybatis.dto.ContentDto">
		SELECT * FROM Mybatis_Bbs ORDER BY id DESC
	</select>
	
	<insert id="writeDao">
		INSERT INTO Mybatis_Bbs (id, writer, content) VALUES (Mybatis_Bbs_SEQ.NEXTVAL, #{param1}, #{param2})
	</insert>
	
	<delete id="deleteDao">
		DELETE FROM Mybatis_Bbs WHERE id = #{param1}
	</delete> -->
	
	
	<!-- <select id="getTotalCount" resultType="Integer">
		select count(product_id) from product
	</select> -->

	
	
</mapper>
